#include<prodcons.h>
void rec(struct procent *b)
{
	int r=0;
	int s=1;
	int g=1;
	//printf("%d%d%d",r,s,g);
	
	//to print the contents of the stack
	/*track=track1;
	while((int)((int)track1-(int)track)<=track1->prstklen)
	{
	   ++track;
	   printf("%p\n",*track);
	}
	sleep(1);

	return;*/


	
	/*int x=1;
		
	char *add;
	struct procent *st=NULL;
	struct procent *base=NULL;
	base=&proctab[getpid()];
	st=&proctab[getpid()];

	st=st->prstkptr;
	printf("Final value of stack pointer %u\n",st);


	base=base->prstkbase;
	printf("Final value of base pointer %u\n",base);
	
	i=(int)base-(int)st;
	printf("\nPID: %d",getpid());
	add=getmem(1024);
	printf("ADDRESS %u",add);	
	kill(getpid());
	return;*/



//printf("ENtered the process");
	
}
